Read Me.

Folders:

Utils= Conexion to SQL

Templates=CSS and HTML the Front

Routes= URL the users have acces to the link, I used Blueprints to separate the app into many parts and have control of all theroute used.

Models= Save the data used.


Index.py
Run the app

app.py
Config of the app

autopep8
Format code

SQLAlchemy to conect to the data base.

I used pip install python-dotenv to install python-dotenv and used it to config environment variables